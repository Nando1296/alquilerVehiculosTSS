# alquilerVehiculosTSS

#Instrucciones para ejecutar el programa

1. Ubicarse en la carpeta 'frontweb'
2. Instalar las dependencias con el comando 'npm install'
2. Iniciar el proyecto con el comando 'npm start'
